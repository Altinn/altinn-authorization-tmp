<Project>

    <!-- Add default target props here before import. In order for imported targets props from sub-projects to override any defaults set here.  -->

    <Import Project="..\Directory.Build.targets" />
</Project>