meta {
  name: DELETE_Delegated_supplier_Org_For_Consumer
  type: http
  seq: 7
}

delete {
  url: {{baseUrl}}/accessmanagement/api/v1/enduser/maskinportenconsumers?party={{supplierParty}}&consumer={{consumerOrg}}
  body: none
  auth: inherit
}

params:query {
  party: {{supplierParty}}
  consumer: {{consumerOrg}}
  ~resource: {{resource}}
}

tests {
  test(bru.getVar("requestName"), function() {
    const data = res.getBody();
    
    expect(res.status).to.equal(204);
    assert.isNotOk(data, "Expected response body to be empty");
    
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
