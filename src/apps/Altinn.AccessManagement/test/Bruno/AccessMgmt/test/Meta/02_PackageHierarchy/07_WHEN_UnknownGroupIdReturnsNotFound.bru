meta {
  name: 07_WHEN_UnknownGroupIdReturnsNotFound
  type: http
  seq: 7
}

get {
  url: {{baseUrl}}/accessmanagement/api/v1/meta/info/accesspackages/group/:id
  body: none
  auth: none
}

params:path {
  id: 00000000-0000-0000-0000-000000000000
}

docs {
  WHEN an unknown group id is requested, THEN the response is 404.
}

tests {
  test("WHEN the group id is unknown, the request returns 404", function() {
    expect(res.status).to.equal(404);
  });

  test("THEN the response carries a problem body", function() {
    const body = res.getBody();
    expect(body).to.have.property("status", 404);
  });
}
