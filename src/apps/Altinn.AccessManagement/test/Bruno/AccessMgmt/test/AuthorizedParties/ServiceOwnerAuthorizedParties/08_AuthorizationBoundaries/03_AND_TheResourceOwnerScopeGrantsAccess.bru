meta {
  name: 03_AND_TheResourceOwnerScopeGrantsAccess
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/accessmanagement/api/v1/resourceowner/authorizedparties
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
  Accept: application/json
}

body:json {
  {
    "type": "urn:altinn:person:identifier-no",
    "value": "{{subjectPersonId}}"
  }
}

docs {
  AND the resource owner scope is one the policy accepts, so the same request succeeds.
}

script:pre-request {
  const testdata = require(`./test/AuthorizedParties/testdata/${bru.getEnvVar("tokenEnv")}.js`);
  const sharedtestdata = require(`./testdata/sharedtestdata.js`);
  const { loginAsServiceOwner, scopes } = require("./test/AuthorizedParties/ServiceOwnerAuthorizedParties/helpers.js");

  bru.setVar("subjectPersonId", testdata.REGN_ULASTELIG_RETTFERDIG_TIGER.dagligleder.pid);

  await loginAsServiceOwner(sharedtestdata.serviceOwners.digdir, scopes.authorizedPartiesResourceOwner);
}

tests {
  test("AND the resource owner scope grants access with 200", function() {
    expect(res.status).to.equal(200);
  });
}
