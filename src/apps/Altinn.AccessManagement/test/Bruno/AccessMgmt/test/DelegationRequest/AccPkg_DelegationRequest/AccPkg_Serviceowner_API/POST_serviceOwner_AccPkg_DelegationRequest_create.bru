meta {
  name: POST_serviceOwner_AccPkg_DelegationRequest_create
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/accessmanagement/api/v1/serviceowner/delegationrequests/package
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
  Accept: application/json
}

body:json {
  {
  "from": "urn:altinn:person:identifier-no:{{from}}",
    "to": "urn:altinn:organization:identifier-no:{{toOrg}}",
    "package": "{{package}}"
  }
  
}

script:pre-request {
  const sharedtestdata = require(`./testdata/sharedtestdata.js`);
    const testdata = require(`./testdata/be-om-tilgang/${bru.getEnvVar("tokenEnv")}.js`);
   
    bru.setVar("from", testdata.Bot_person_serviceowner.pid);
    bru.setVar("toOrg", testdata.Bot_package_Org.org_no);
    bru.setVar("package", testdata.package_to_delegate.package_id_virksomhet);
    
    var getTokenParameters = {
      auth_org: sharedtestdata.serviceOwners.ttd.org,
      auth_orgNo: sharedtestdata.serviceOwners.digdir.orgno,
      auth_tokenType: sharedtestdata.authTokenType.enterprise,
      auth_scopes: sharedtestdata.auth_scopes.serviceownerDelegationRequestWrite
    }
    
    const testTokenGenerator = require(`./TestToolsTokenGenerator.js`);
    const token = await testTokenGenerator.getToken(getTokenParameters);
    bru.setVar("bearerToken", token);
}

script:post-response {
  const data = res.getBody();
    expect(data).to.exist;
      expect(data.id).to.exist;
    bru.setEnvVar("tilgangspakkequestId", data.id);
}

tests {
  const testdata = require(`./testdata/be-om-tilgang/${bru.getEnvVar("tokenEnv")}.js`);
    
    test("Person_A request for access on Access package to Org_user via serviceowner API", function() {
      const data = res.getBody();
      
      expect(res.status).to.equal(202);
      expect(data).to.exist;
      expect(data.id).to.exist;
      expect(data.status).to.exist;
      
       // 2. Verify and save requestId
     expect(data).to.have.property("id");
     bru.setEnvVar("tilgangspakkeserviceowner_requestId", data.id);
      
      // 3. Verify and save status
    expect(data).to.have.property("status");
    bru.setEnvVar("expectedStatus", data.status);
      
    });
}

settings {
  encodeUrl: true
  timeout: 0
}
