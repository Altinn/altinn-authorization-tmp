meta {
  name: POST_Enduser_Connections_Person
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/accessmanagement/api/v1/enduser/connections?party={{party}}&from={{party}}
  body: json
  auth: inherit
}

params:query {
  party: {{party}}
  from: {{party}}
}

headers {
  Accept: application/json
}

body:json {
  {
    "personidentifier": "{{toPersonId}}", // fnr or username
    "lastName": "{{toPersonLastName}}"
  }
}

script:pre-request {
  const sharedtestdata = require(`./testdata/sharedtestdata.js`);
  
  bru.setVar("requestName", "POST_Enduser_Connections_Person");
  
  bru.setVar("party", );
  bru.setVar("toPersonId", );
  bru.setVar("toPersonLastName", );
  
  var getTokenParameters = {
    auth_userId: "",
    auth_partyId: "",
    auth_partyUuid: "",
    auth_ssn: "",
    auth_tokenType: sharedtestdata.authTokenType.personal,
    auth_scopes: sharedtestdata.auth_scopes.portalEnduser
  }
  
  
const testTokenGenerator = require(`./TestToolsTokenGenerator.js`);
const token = await testTokenGenerator.getToken(getTokenParameters);
  bru.setVar("bearerToken", token);
}
