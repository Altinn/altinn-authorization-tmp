meta {
  name: SysRes_EnduserAccMgmt_AsHadm_Permit
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/authorization/api/v1/decision
  body: json
  auth: none
}

headers {
  Content-Type: application/json
  Ocp-Apim-Subscription-Key: {{apimSubscriptionKey}}
}

body:json {
  /*
  See Docs tab for test case description
  */
  {
      "Request": {
          "ReturnPolicyIdList": false,
          "AccessSubject": [
              {
                  "Attribute": [
                      {
                          "AttributeId": "urn:altinn:userid",
                          "Value": "{{subjectUserId}}"
                      }
                  ]
              }
          ],
          "Action": [
              {
                  "Attribute": [
                      {
                          "AttributeId": "urn:oasis:names:tc:xacml:1.0:action:action-id",
                          "Value": "write",
                          "DataType": "http://www.w3.org/2001/XMLSchema#string"
                      }
                  ]
              }
          ],
          "Resource": [
              {
                  "Attribute": [
                      {
                          "AttributeId": "urn:altinn:resource",
                          "Value": "{{resourceId}}"
                      },
                      {
                          "AttributeId": "urn:altinn:partyid",
                          "Value": "{{resourcePartyId}}",
                          "DataType": "http://www.w3.org/2001/XMLSchema#string"
                      }
                  ]
              }
          ]
      }
  }
}

script:pre-request {
  const sharedtestdata = require(`./testdata/shared.js`);
  const testdata = require(`./testdata/Authorization/${bru.getEnvVar("tokenEnv")}.js`);
  
  bru.setVar("requestName", "SysRes_EnduserAccMgmt_AsHadm_Permit");
  bru.setVar("resourceId", sharedtestdata.systemResources.enduser_accessmanagment);
  bru.setVar("subjectUserId", testdata.REGN_ULASTELIG_RETTFERDIG_TIGER.a2_hovedadministrator.userId);
  bru.setVar("resourcePartyId", testdata.REGN_ULASTELIG_RETTFERDIG_TIGER.partyId);
  
}

tests {
  test("Decision_" + bru.getVar("requestName"), function() {
    const data = res.getBody();
    expect(res.status).to.equal(200);
    expect(data.response[0]).to.have.property('decision', "Permit");
  });
}
