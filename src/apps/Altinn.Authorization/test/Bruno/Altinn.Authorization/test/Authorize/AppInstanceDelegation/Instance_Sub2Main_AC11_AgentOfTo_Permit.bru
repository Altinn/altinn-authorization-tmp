meta {
  name: Instance_Sub2Main_AC11_AgentOfTo_Permit
  type: http
  seq: 18
}

post {
  url: {{baseUrl}}/authorization/api/v1/authorize
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
  Ocp-Apim-Subscription-Key: {{apimSubscriptionKey}}
}

body:json {
  {
      "Request": {
          "ReturnPolicyIdList": false,
          "AccessSubject": [
              {
                  "Attribute": [
                      {
                          "AttributeId": "urn:altinn:person:identifier-no",
                          "Value": "{{subject}}"
                      }
                  ]
              }
          ],
          "Action": [
              {
                  "Attribute": [
                      {
                          "AttributeId": "urn:oasis:names:tc:xacml:1.0:action:action-id",
                          "Value": "read"
                      }
                  ]
              }
          ],
          "Resource": [
              {
                  "Attribute": [
                      {
                          "AttributeId": "urn:altinn:resource:instance-id",
                          "Value": "urn:altinn:instance-id:{{partyId}}/{{instanceId}}"
                      }
                  ]
              }
          ]
      }
  }
}

assert {
  ~res.status: eq 200
  ~res.body: contains created
}

script:pre-request {
  const sharedtestdata = require(`./testdata/shared.js`);
  const testdata = require(`./testdata/InstanceDelegation/${bru.getEnvVar("tokenEnv")}.js`);
  
  // Should be the same as the .bru request file. Used as Test-name Tests tab and shows in test result.
  bru.setVar("requestName", "Instance_Main2Sub_AC10_DaglOfTo_Permit");
  
  bru.setVar("instanceId",  testdata.instances.sub2mainAgent.instanceId);
  bru.setVar("subject",  testdata.organizations.legitimJusterbarTiger.agent.personId);
  bru.setVar("partyId",  testdata.organizations.slakkAktivBilleIKS.subunits.slakkAktivBilleIKS.partyId);
  
  // Underenhet A har instansiert en app og er klient hos hovedenhet B, så agent bruker med riktig klientdelegering/tilgangspakke hos B skal få Permit på den instansen.
  
  var getTokenParameters = {
    auth_tokenType: sharedtestdata.authTokenType.enterprise,
    auth_scopes: sharedtestdata.auth_scopes.authorize,
    auth_org: sharedtestdata.serviceOwners.ttd.org,
    auth_orgNo: sharedtestdata.serviceOwners.ttd.orgno,
  }
  
  
  const testTokenGenerator = require(`./TestToolsTokenGenerator.js`);
  const token = await testTokenGenerator.getToken(getTokenParameters);
  bru.setVar("bearerToken",  token);
}

tests {
  test(bru.getVar("requestName"), function() {
    const data = res.getBody();  
    expect(res.status).to.equal(200);
    expect(data.response[0]).to.have.property('decision', "Permit");
  });
}
